<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".SlicePosApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SlicePOS"
        tools:targetApi="31">
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.admin.AdminDashboardActivity"/>
        <activity android:name=".ui.cashier.CashierDashboardActivity"/>
        <activity android:name=".ui.cashier.NewOrderActivity"/>
        <activity android:name=".ui.cashier.MenuActivity"
            android:exported="false"
            android:label="Menu" />
        <activity android:name=".ui.cashier.ActiveOrdersActivity"
            android:exported="false"
            android:label="Active Orders" />
        <activity android:name=".ui.cashier.PaymentActivity"/>
        <activity android:name=".ui.cashier.PaymentSuccessActivity"/>
        <activity android:name=".ui.cashier.CartActivity"/>
        <activity android:name=".ui.cashier.OrderDetailsActivity"/>
        <activity android:name=".ui.cashier.OrderQueueActivity"/>
        <activity android:name=".MainActivity"/>
    </application>

</manifest>